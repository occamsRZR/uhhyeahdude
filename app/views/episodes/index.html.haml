%h2 Episode Archive
= paginate @episodes
%table.table.table-striped.table-hover.table-responsive
  %th Epi Number
  %th Date
  - @episodes.each do |episode|
    %tr.episode
      %td.episode-name= link_to "Episode #{episode.number}", episode_path(episode)
      %td.episode-date= episode.published_at.strftime("%d %b %y")
= paginate @episodes
